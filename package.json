{
    "name": "infinity-quiz-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "install-modules": "cd frontend && npm install && cd ../backend && npm install"
    },
    "workspaces": [
        "frontend",
        "backend"
    ]
}